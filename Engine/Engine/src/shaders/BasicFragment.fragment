#version 330 core

out vec4 FragColor;

in vec2 TexCoord;

uniform sampler2D ourTexture;

void main()
{
	vec4 texColour = texture(ourTexture, TexCoord);
	if(texColour.a < 0.1)
		discard;
	FragColor = texColour;
}